{
  "name": "task-management-platform",
  "version": "1.0.0",
  "description": "Task & Team Management Platform",
  "scripts": {
    "install-server": "cd server && npm install",
    "build": "cd server && npm install",
    "start": "cd server && node server.js"
  }
}
