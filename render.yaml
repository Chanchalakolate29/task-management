services:
  - type: web
    name: taskflow-backend
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: supersecret_jwt_key_task_manager_2026_spec
